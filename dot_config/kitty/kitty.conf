# Theming
# include ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf
include ./matugen.conf

# Font
font_family      JetBrains Mono Nerd Font
font_size 11.0

# Cursor
cursor_shape beam
cursor_trail 1
cursor_blink_interval 0.5 ease-in-out

#Scrolling
pixel_scroll yes
momentum_scroll 0.98
touch_scroll_multiplier 5.0

# Padding (why weird value? consistency with foot)
window_margin_width 15

# No stupid close confirmation
confirm_os_window_close 0

# Use fish shell
shell fish

# Tab bar
tab_bar_style custom
tab_separator "  "
tab_title_max_length 10
tab_bar_min_tabs     1

# Copy
map ctrl+c    copy_or_interrupt

# Search
map ctrl+f   launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id
map kitty_mod+f   launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id

#Open new Tab with same cwd
map ctrl+t launch --cwd=current --type=tab

# Scroll & Zoom
map page_up    scroll_page_up
map page_down    scroll_page_down

map ctrl+plus  change_font_size all +1
map ctrl+equal  change_font_size all +1
map ctrl+kp_add  change_font_size all +1
map ctrl+minus       change_font_size all -1
map ctrl+underscore       change_font_size all -1
map ctrl+kp_subtract       change_font_size all -1
map ctrl+0 change_font_size all 0
map ctrl+kp_0 change_font_size all 0

# Unbind Shift+Ctrl+Arrow keys
map shift+ctrl+up no_op
map shift+ctrl+down no_op
map shift+ctrl+left no_op
map shift+ctrl+right no_op

# Resize windows with Shift+Alt+hjkl (vim-style)
map ctrl+shift+h resize_window narrower
map ctrl+shift+l resize_window wider
map ctrl+shift+k resize_window taller
map ctrl+shift+j resize_window shorter

# Switch to next layout
map ctrl+shift+backslash next_layout

# Go to numbered tab
map ctrl+1 goto_tab 1
map ctrl+2 goto_tab 2
map ctrl+3 goto_tab 3
map ctrl+4 goto_tab 4
map ctrl+5 goto_tab 5
map ctrl+6 goto_tab 6
map ctrl+7 goto_tab 7
map ctrl+8 goto_tab 8
map ctrl+9 goto_tab 9
map ctrl+10 goto_tab 10


